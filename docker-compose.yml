version: '3.8'

services:
  sahayasetu:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sahayasetu-app
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
    restart: unless-stopped
